/**
 * URLs for live demo iframes embedded in presentation slides.
 * Edit these before presenting to match your local environment.
 */
export const demoUrls = {
  /** The fullstack-test-app running locally (Meet the App slide) */
  app: 'http://localhost:4200',
  /** Claude Code Viewer dashboard (both demo slides) */
  viewer: 'http://localhost:3400',
}
